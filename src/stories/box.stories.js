import React from 'react';
import { Box } from '../';

export default {
  title: 'Layout | Box',
  component: Box,
};

export const Default = () => (
  <Box p={4} color="white" bg="secondary">
    test
  </Box>
);
