import React from 'react';
import { Badge } from '../';

export default {
  title: 'Components | Badge',
  component: Badge,
};

export const Default = () => <Badge variant="primary">Badge</Badge>;

export const Outline = () => <Badge variant="outline">Badge</Badge>;
